# Personal Website — Claude Context

## Project
Max Delaporte's personal website. Single-page with hero, about, portfolio, and footer sections.

Key files:
- `index.html` — main HTML
- `1_css/style.css` — all styles (single stylesheet)
- `2_js/script.js` — scroll, nav, portfolio tab logic
- `2_js/ascii-bg.js` — ASCII background animation

## Design References

### JobTracker Mobile App Design
Local files: `design-refs/jobtracker-mobile-app-design/`

Key files:
- `chats/` — design conversation transcripts (read these for intent)
- `project/Jobtracker Mobile (Legacy).html` — primary mobile design reference
- `project/tokens.css` — design tokens (colors, spacing, typography)
- `project/styles.css` — shared component styles
- `project/legacy-mobile/` — legacy mobile component files

When working on mobile styles, read the design files above and compare against `@media (max-width: 768px)` in `1_css/style.css`.
